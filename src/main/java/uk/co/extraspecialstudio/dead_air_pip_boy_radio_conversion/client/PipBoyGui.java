package uk.co.extraspecialstudio.dead_air_pip_boy_radio_conversion.client;

/**
 * Marker for Pip-Boy shell screens (standalone or Dead Air-backed) so raise-pose / LCD
 * capture can treat them the same without loading optional Dead Air classes.
 */
public interface PipBoyGui {
}
